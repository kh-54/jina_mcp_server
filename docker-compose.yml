services:
  jina-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      MCP_PORT: 8000
      MCP_HOST: 0.0.0.0
      MCP_AUTH_TOKEN: ${MCP_AUTH_TOKEN:-}
      MCP_ALLOWED_HOSTS: ${MCP_ALLOWED_HOSTS:-}
    restart: unless-stopped
